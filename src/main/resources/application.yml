spring:
  application:
    name: dosys-platform-backend
  datasource:
    url: jdbc:postgresql://aws-1-us-east-2.pooler.supabase.com:5432/postgres?sslmode=require
    username: postgres.fbqkqtgkbnlbfwwmkiie
    password: SUPABASEPASSWORD123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration

springdoc:
  swagger-ui:
    path: /swagger-ui.html

app:
  jwt:
    secret: DOSYS_DEV_SECRET_CHANGE_THIS_TO_A_LONG_SECURE_SECRET_123456
    expiration: 86400000
